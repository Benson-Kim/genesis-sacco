{
  "name": "@genesis/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAPI-generated types + Zod-validated, TanStack-Query-friendly client functions. Never hand-write request/response types here — see scripts/generate.mjs (gate 1.1).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate:client": "node scripts/generate.mjs",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
