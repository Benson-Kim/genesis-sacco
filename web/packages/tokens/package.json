{
  "name": "@genesis/tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Design tokens extracted verbatim from genesis_prestige_app.html :root CSS variables (MASTER_PROMPT gate 1.1). Do not hand-edit color/spacing values elsewhere in the codebase — import from here.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}