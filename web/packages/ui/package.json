{
  "name": "@genesis/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Design-system component package (gate 1.1). Views must compose these instead of styling ad hoc.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@genesis/api-client": "*",
    "@genesis/tokens": "*"
  },
  "peerDependencies": {
    "react": "^18.3.0",
    "@tanstack/react-query": "^5.59.0"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@types/react": "^18.3.11",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
